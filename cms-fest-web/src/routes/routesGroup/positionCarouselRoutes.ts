import { PositionCarousel } from "@/screens/PositionCarousel/PositionCarousel";
import PATHS from "../paths";

export const positionCarouselRoutes = [
  {
    path: PATHS?.position?.index,
    component: PositionCarousel,
  },
];
